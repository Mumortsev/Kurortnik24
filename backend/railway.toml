[build]
builder = "NIXPACKS"

[deploy]
startCommand = "python -m uvicorn api.main:app --host 0.0.0.0 --port $PORT & python -m bot.main"
healthcheckPath = "/api/health"
healthcheckTimeout = 100
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
