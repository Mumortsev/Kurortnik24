meta:
  environment: docker
  toolchain:
    name: docker
    version: latest

build:
  dockerfile: Dockerfile

run:
  persistence:
    - mountPoint: /data
      name: shop-data

ports:
  - port: 80
    type: http
